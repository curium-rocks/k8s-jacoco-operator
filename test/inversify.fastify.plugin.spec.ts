import { describe, it } from '@jest/globals'
import 'reflect-metadata'

describe('inversify.fastify.plugin', () => {
  it('Should expose root container to requests', () => {

  })
  it('Should expose request scope to requests', () => {

  })
  it('Should optionally cleanup scope on response', () => {

  })
  it('Should optionally cleanup scope on close', () => {

  })
  it('Should not cleanup root scope by default', () => {

  })
  it('Should not cleanup request scope by default', () => {

  })
})
